# Configuration for AMI memory system
# Creation date: April 18, 2025
# Project: F.A.M.I.L.Y. (Framework for Artificial Mind's Identity Long-term memorY)

# Main connection parameters
FAMILY_DB_HOST=localhost
FAMILY_DB_PORT=5432
FAMILY_DB_NAME=family_db

# Database administrator user
FAMILY_ADMIN_USER=family_admin
FAMILY_ADMIN_PASSWORD="Cold68#Fire"

# AMI user (for standard operations)
FAMILY_AMI_USER=ami_user
FAMILY_AMI_PASSWORD=ami_secure_password

# Additional parameters
FAMILY_TEST_MODE=false

# Parameters for supporting "Unknown" category and primacy paradox
FAMILY_UNKNOWN_PARTICIPANT_ID=1  # ID of "UNKNOWN" record in participants table
FAMILY_ENABLE_PROVISIONAL_ENTITIES=true  # Enable temporary entities mechanism
FAMILY_AUTO_RECONCILE=true  # Automatic linking of temporary entities
FAMILY_RECONCILE_INTERVAL=3600  # Interval for linking temporary entities (in seconds)

# Parameters for vector operations
FAMILY_VECTOR_MODEL=all-MiniLM-L6-v2  # Text vectorization model
FAMILY_VECTOR_DIMENSIONS=384  # Vector dimensions for the model
FAMILY_VECTOR_BATCH_SIZE=32  # Batch size for vectorization

# Parameters for service layer
FAMILY_MOCK_EXTERNAL_SERVICES=false  # No mocks in production
FAMILY_ENABLE_TRANSACTION_LOGGING=false  # No transaction logging in production
